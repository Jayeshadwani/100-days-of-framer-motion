1. Truncate the text if exceeding available width using css properties
white-space: nowrap;
textOverflow: clip;
display: inline-block;
max-width: 100%;

2. Play with transition property (easeOut, easeInOut) and opacity for visible text used an array in incremental order [0.5, 0.7, 1]

